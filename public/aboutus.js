function pic (name) {
    var img = document.createElement("img");
    img.scr = `../images/${name}.png`;
    img.width = 500;
    img.height - 500;
    // if (name == "Dara") {

    // }

    document.body.appendChild(img);
}